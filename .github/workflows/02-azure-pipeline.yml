trigger: none

pool:
  name: Default

steps:
- checkout: self

- task: NodeTool@0
  inputs:
    versionSpec: '24.x'

- script: |
    npm install -g yarn
    yarn
    npx playwright install --with-deps
  displayName: 'Instalar dependências'

- script: yarn run e2e
  displayName: 'Executar testes'